spring:
  datasource:
    url:  jdbc:postgresql://localhost:5432/patient_system
    username: postgres
    password: postgres

spring.jpa.properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto: create-drop
